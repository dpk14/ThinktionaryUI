'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactNative = require('react-native');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

var styled = function styled(Component) {
  var comp = function comp(styles) {
    return function (_ref) {
      var style = _ref.style,
          children = _ref.children,
          rest = _objectWithoutProperties(_ref, ['style', 'children']);

      var composed = [];
      if (styles) {
        composed = [].concat(_toConsumableArray(composed), [_reactNative.StyleSheet.create({
          styles: styles
        }).styles]);
      }

      if (Array.isArray(style)) {
        composed = [].concat(_toConsumableArray(composed), _toConsumableArray(style));
      } else if (style) {
        composed = [].concat(_toConsumableArray(composed), [_reactNative.StyleSheet.create({
          style: style
        }).style]);
      }

      return _react2.default.createElement(
        Component,
        _extends({ style: composed }, rest),
        children
      );
    };
  };

  return function (arg) {
    if (typeof arg === 'function') {
      return comp(arg());
    }

    return comp()(arg);
  };
};

exports.default = styled;